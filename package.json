{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  },
  "scripts": {
    "build": "npx lerna run build",
    "build:image-editor": "npx lerna run --scope tui-image-editor bundle",
    "build:react": "npx lerna run --scope @toast-ui/react-image-editor build",
    "build:vue": "npx lerna run --scope @toast-ui/vue-image-editor build"
  }
}
